import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
    palette: {
        primary: {
            main: "#D1EDF1",
            light: "#E3F3FD",
            dark: "#DAE8FC",
        },
        secondary: {
            main: '#036897',
        }
    },
});